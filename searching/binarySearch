int binarySearch(vector<int>& v, int e) {
    int l = 0, r = v.size() - 1;
    while (l <= r) {
        int m = l + (r - l) / 2;
        if (v[m] == e) return m;
        v[m] < e ? l = m + 1 : r = m - 1;
    }
    // No element exists
    return -1;
}
